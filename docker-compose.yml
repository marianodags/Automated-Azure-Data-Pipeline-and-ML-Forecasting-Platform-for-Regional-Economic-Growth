version: '3.8'

services:
  web_api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./regional_economy.db
      - ENVIRONMENT=production
    volumes:
      - ./data:/app/data
    restart: always
